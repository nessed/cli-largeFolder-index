# STOP_REASON: CORPUS_BASELINE_CHANGED

Run: 20260912T214913Z-7864bc46
Stage reached: 1 (1.3 -- immutable baseline)

## What failed

corpus_15000 (the top rung) has **15012 files**; BUILD_PROMPT.md Stage 1.3 and
SOLUTION.md both record the expected, planning-time count as **15010**. Per
the contract: "if different, stop CORPUS_BASELINE_CHANGED, without repairing it."
No repair, rename, deletion or regeneration of corpus content was performed.

## What the diff against the old shared index (corpus-lab/02_stacks/s2_fts5/harness_15000.db, files table, 15010 rows) shows

Added relative to that index: nul, rg_err.log
Removed relative to that index: (none)

## Best-evidence hypothesis (not corpus content, not confirmed, not acted on)

Both added names ("nul" and "rg_err.log") are consistent with the exact hazard this
same contract warns about in its fixed rules ("Never rely on Git Bash's `rg` shim"):
an `rg` invocation inside corpus_15000 whose stderr/redirect target landed on disk
(`rg_err.log`, and a Windows `> nul` redirect that created a literal file named
`nul` instead of discarding output). This looks like leftover tooling debris from
an earlier diagnostic session, not a regeneration of the fixture and not new
research content. This hypothesis is reported for Ali to judge -- it is not treated
as established, and nothing was deleted to act on it.

## All other rungs

corpus_500, corpus_2000, corpus_5000 file counts matched their ladder sizes exactly
(500 / 2000 / 5000), with no added or removed paths detected.

## What is safe

- 0 stacks were ever installed in any rung this run (verified before and after).
- The original 15,010 files are all still present and were not touched -- only two
  extra, unrelated filenames showed up on top of them.
- No corpus content was modified, deleted, renamed or regenerated by this run.
- $0 metered spend, 0/175 sessions used.

## Resume

Ali (or a future run) can inspect and, if appropriate, manually remove
`harness/corpus_15000/nul` and `harness/corpus_15000/rg_err.log` himself outside
any automated run -- that is a decision this build is not authorized to make.
Once the top-rung count reads back as exactly 15,010 (or BUILD_PROMPT.md's expected
count is deliberately revised by Ali), resume with:

    .venv\Scripts\python.exe _private\evidence_v1\audit.py stage1

This mints a fresh run id and starts Stage 1 over; it does not reuse this run's
budget consumption (none was spent).
