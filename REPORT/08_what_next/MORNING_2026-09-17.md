# Morning note — 2026-09-17

Night of 2026-09-16, Plan E (trust and reproducibility). No retrieval change, no holdout look.

1. **Scorer v3 — Gate T: `GATE_T_STOP`.** 4 of 8 rows passed (T1, T4, T5, T8); T2, T3, T6, T7 failed. T2/T3 failed on a false premise in my own spec: it assumed the P8 recordings contained no loop opens and no interrupted answers; v3 finds 5 fragments and 11 loop-only opens there. T6/T7 are the `cited_unopened` rows, which moved by design. v3 was **not** modified after the STOP.
2. **Phase 9 corrected, v2 → v3.** Sonnet equivalence 13/17 → **13/17**; Opus 14/17 → **14/17** (unchanged under two different citation rules). `value_correct` 5/17 → **5 of 5 keyed** for both. `cited_unopened` Sonnet 1 → **1**, Opus 7 → **4**. **Every Phase 9 STOP verdict stands unchanged**: they were adjudicated under the pre-registered v2 scorer on conditions v3 does not revisit.
3. **Answer key.** `_private/harness_keys/answer_key_v2_2026-09-16.json`, sha256 `773ab75e5018ec58…`, derived by script from the generator's placement registry, no value written by hand. Covers expected year-by-year vectors for 9 trajectory and multi-branch questions. `answer_key.json` was not edited. The 8 remaining answerable questions stay unscorable by value.
4. **Portable default builder is v1 — yes.** `PACKAGE_MANIFEST.json` reads `"default_builder": "v1"`. `--builder v2` now prints `EXPERIMENTAL SHELF V2 -- failed Gate S 2026-09-15; not production` and stamps `builder: v2 (experimental)` into the build manifest.
5. **Clean build A vs B — Gate R1: `GATE_R1_STOP`,** 7 of 8 rows passed. Sole failure R1.3: `canonical_export.json` differs. Cause is `pages.files.file_id`, a surrogate key assigned by filesystem walk order — 255 of 500 rows share `rel`, `sha256`, `size`, `n_pages`, `status`, `error_class` and differ only in it; dropping that column makes the table identical. All four shelf tables byte-identical. **Vector determinism class: `deterministic`** for both cards (472×384) and captions (4483×384) — rounded-to-3dp hashes equal.
6. **`corpus_2000` — Gate R2: PASS,** 6 of 6. 2000 files indexed in 375s, self-test 18/18, the one headless Sonnet answer cited a page the transcript shows as `OPENED`, guard did not fire, 0 memory files before and after. **This is the same fixture ecosystem** — a second folder from the same generator, not a second kind of folder.
7. **Failed gates and unexplained mismatches:** Gate T `GATE_T_STOP` (rows T2, T3, T6, T7 — T2/T3 on a false spec premise, T6/T7 by design). Gate R1 `GATE_R1_STOP` (row R1.3 — surrogate row id, diagnosed, nothing downstream reads it). `c_score_live_v3.py` cannot see a session killed by an account quota: `sessions_lost` reads 0 for P10O1 beside accounting that reads 17 of 20. Probe-session cost is not recorded by the probe harness — **budget line unreconciled by roughly $0.30, candidate cause: `phase9_probes__*.json` carries no `cost_usd` field.**
8. **Sonnet ×3 (v3 equivalence / driver cost):** P10S1 **13/17** $11.0699 · P10S2 **11/17** $10.0502 · P10S3 **11/17** $10.7911. With P9S (13/17) the four-run **range is 2** under v3 and **5** under v2 (13, 14, 10, 9). Only **10 of 17** questions held the same verdict in all four runs.
9. **Opus (v3 equivalence / driver cost):** P10O1 **12/17** $20.5743 — **partial**: 3 of 20 sessions (`tr_07`, `tr_10`, `tr_16`) cut by the account's own session limit, all 3 inside the answerable 17, all 3 scored as misses. With P9O (14/17) the apparent range of 2 is mostly that quota: 3 of the 4 questions that changed between the two Opus runs are exactly those 3. **Opus variance remains unmeasured.** `P10O2` was cut.
10. **Cost.** Live tonight **$52.49** of the **$95** hard cap: Sonnet $31.92 of $45 (third battery started at $21.12, under the $30 bar), Opus $20.57 of $40, other ≈$0.30 unrecorded. Sessions: **80 live** of the ≤94 allowance, plus 12 probes. **Holdout looks: 0.**
11. **Retries, timeouts, kills, contamination, interventions.** Timeouts **0**. Max-turns kills **0**. Contamination events **0** (quarantine scanned 872 transcripts, moved 0; 0 memory files under every corpus project key; 0 dev-repo reads in the R3 trace across 891 distinct paths). Account rate-limit kills **3** (`tr_07`, `tr_10`, `tr_16`). Battery reruns **0**. Manual interventions **4**: Phase 2 re-run in full after `run_record.py` correctly refused a record opened after its work (deterministic, numbers reproduced exactly); two background tasks racing one clean room, killed and re-run alone; R3 tracer rewritten twice (deadlock, then an escape bug — both produced an empty log a naive gate would have read as a pass); a hand-written `_TRAJECTORY_FILLER` word list I invented and caught myself before it could touch retrieval, replaced with the existing 12-word set and re-verified.
12. **Branch `phase10-trust-and-repro`.** Commits tonight: `87e7b5b` `9da8744` `20e3d7f` `fc23d31` `5bbbe10` `527ef66` `e442e6d` `1b217ff` `8e57705` `c4dce54` `f864d14` `d5849bb` `02f53c8` `8df11ee` `5f1c9e0` `6052589` (+ the Phase 10.5 commit carrying this note). Tag: **`scorer-v3-frozen-2026-09-16`**, sha256 `09f4105d81114dee…`. **Nothing was pushed.**
13. **Working tree:** clean after the Phase 10.5 commit. Rung `harness/corpus_15000` clean — no `CLAUDE.md`, no `.claude`. No stray `node.exe` or `python.exe` from this work.
14. **What changed.** Four production files, all additive, all defaults preserving prior behaviour: `c_live_battery.py` `--capture full` (default `last`); `setup_folder.py` `--builder v1|v2` (**default v1**), `--artefacts`, `--seed-env`, `ask --stream-json`, and the manifest trio written at end of install; `c_shelf.py` imports `_TRAJECTORY_FILLER` from the new `trajectory_filler.py` instead of `c_offline_gate.py` (same 12-word set, verified the same object, B2c still 52/57); `c_stack.py` **deny-list widened** to cover the whole `00_archive/` directory after `ACCEPTANCE.md` moved into it — this one is not a flag, it denies strictly more, and it was made before Phase 10 began. `c_stop_guard.py` and `c_selftest.py` are byte-identical to Phase 9. New: `run_record.py`, `docs_check.py`, `c_score_live_v3.py`, `c_key_v2_build.py`, `make_portable.py`, `build_manifest.py`, the `p10_*` harness, `trajectory_filler.py`, and 106 tests (all green).
15. **What did not change.** Shelf `s7_shelf` v1, `CLAUDE_MD_TEMPLATE`, the citation guard and its `--v2` behaviour, the ranker, B2c, every prompt, `answer_key.json` (key v1), and **every Phase 9 gate verdict**. `checksums.py verify` reads **26 unchanged, 0 changed** against both `s7_phase10_pre` (demo path) and `s7_phase10_live_pre`. No model call was made to score anything, in any phase.
16. **Before the demo, Ali must know:** the published "Opus 14 beats Sonnet 13" reading is **retired** — the gap of 1 is smaller than the instrument's own noise (range 2 under v3, 5 under v2), and the pre-registered rule forbids preferring anything on it; the corrected figures in `CORRECTION_2026-09-16.md` supersede the brief, the demo pack, the night handoff, `INSTALL_FOR_SIR.md` and `README.md`, whose bodies are untouched behind one-line pointers; and **genuine real-world cross-folder generalisation remains untested — every gate here runs on corpora from one generator. The next experiment is the pre-registered adversarial synthetic professor folder**, built by different rules with its own key, its own contamination rule and its own gate, and its README must exist before its first build.
